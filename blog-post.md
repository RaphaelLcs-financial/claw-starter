# 如何用 5 分钟创建 OpenClaw 技能：完整指南

> 想为 OpenClaw 开发技能但不知道从哪开始？本文教你用 @claw-dev/starter 快速创建技能，包含格式验证功能。

---

## 问题：开发 OpenClaw 技能太慢

每次想为 OpenClaw 开发新技能时，都要：

1. 创建目录结构
2. 编写 SKILL.md（frontmatter, metadata 等）
3. 配置 package.json（如果是 npm 包）
4. 测试格式是否正确
5. ...

这个过程至少要 30 分钟，而且容易出错（忘记某个字段、metadata 格式不对）。

## 解决方案：一键生成 + 格式验证

### 第一步：生成技能骨架

```bash
# 交互式创建（推荐）
npx @claw-dev/starter

# 命令行创建
npx @claw-dev/starter create my-awesome-skill \
  --template typescript \
  --description "一个很棒的技能" \
  --emoji "⚡" \
  --author "你的名字"
```

**5 秒完成！** 自动生成：
- 完整的目录结构
- 正确的 SKILL.md 格式
- 配置好的 package.json
- TypeScript/NPM 支持

### 第二步：验证格式

```bash
# 验证生成的 SKILL.md
npx @claw-dev/starter validate

# 验证指定路径
npx @claw-dev/starter validate /path/to/skill/SKILL.md
```

**自动检查：**
- ✅ 必需字段（name, description）
- ✅ Metadata 部分
- ✅ 至少一个实现章节
- ❌ 错误提示，避免运行时失败

### 第三步：开始开发

```bash
cd my-awesome-skill
# 编辑 SKILL.md
# 添加你的代码
# 测试
```

## 三种模板对比

### Basic 模板
```
适合：简单技能，不需要发布到 npm
生成时间：5秒
复杂度：最低
```

### NPM 模板
```
适合：需要分享给其他人使用的技能
生成时间：5秒
包含：package.json, index.js, scripts/
```

### TypeScript 模板 ⭐ 推荐
```
适合：复杂技能，类型安全
生成时间：5秒
包含：tsconfig.json, src/index.ts, dist/ 编译
```

## 实际案例：5 秒创建技能

```bash
# 之前：手动创建（30分钟+）
mkdir my-skill
cd my-skill
# 创建 SKILL.md（frontmatter 格式...）
# 创建 package.json...
# 配置 tsconfig.json...
# 测试格式...

# 现在：一键生成（5秒）
npx @claw-dev/starter create my-skill -t typescript
cd my-skill
# 开始开发！
```

## 格式验证：避免运行时错误

没有验证，你可能会：

```yaml
---
name: MySkill  # ✅
# 忘记 description 了！
metadata:
  openclaw:
    emoji: "🤖"  # ❌ 格式错误，应该是对象
---
```

运行验证后：

```bash
$ npx @claw-dev/starter validate
✗ 验证失败：
  ❌ 缺少必需字段: description
  ❌ Metadata 格式错误: emoji 应该是字符串
```

**立即修复，而不是等到运行时报错！**

## 安装和使用

```bash
# 全局安装（可选）
npm install -g @claw-dev/starter

# 或直接使用 npx
npx @claw-dev/starter create my-skill
```

## 下一步

创建技能后：

1. ✅ 编辑 `SKILL.md` 配置你的技能
2. ✅ 添加你的代码逻辑
3. ✅ 测试技能功能
4. ✅ 发布到 GitHub
5. ✅ （可选）发布到 npm

## 总结

| 功能 | 手动创建 | claw-starter |
|------|---------|-------------|
| 创建时间 | 30分钟+ | 5秒 |
| 格式错误风险 | 高 | 低（验证功能）|
| 学习曲线 | 陡峭 | 平缓 |
| 模板选择 | 无 | 3种 |

**节省时间：每次至少 25 分钟！**

---

**链接：**
- [GitHub Repository](https://github.com/RaphaelLcs-financial/claw-starter) (即将发布)
- [NPM Package](https://www.npmjs.com/package/@claw-dev/starter) (即将发布)

---

_本文由 梦心 原创，分享给 OpenClaw 社区_
